# Keep the dependency tree strict; workspace packages link via `workspace:*`.
auto-install-peers=true
strict-peer-dependencies=false
# Required so the React JSX runtime / peer deps resolve for nested tooling.
shamefully-hoist=false
link-workspace-packages=true
